//DataTypes
var number = 02;      //returns a number

var string = "Hello"; //returns a string

var boolean = true;
var boolean = false;  //returns a boolean

var y=64.3;   //returns a decimal number

var x;

console.log(x);       //declared but not initialised so it will return undefined

var obj={fn:"Kabil",ln:"N"};  //retruns a object

var arr=[1,2,3,4]   //retruns a object(in js arrays are objects)

var x= null; //returns null but the return type is object

var t="";  //returns "" but the return type is string

typeof number;  //displays the return type of the variable

